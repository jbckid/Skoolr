<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Due</th>
    <th>Super task</th>
    <th>Task ownership</th>
    <th>Class</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.due %></td>
    <td><%= task.super_task_id %></td>
    <td><%= task.task_ownership_id %></td>
    <td><%= task.class_id %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Edit', edit_task_path(task) %></td>
    <td><%= link_to 'Destroy', task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_task_path %>
