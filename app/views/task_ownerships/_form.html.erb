<%= form_for(@task_ownership) do |f| %>
  <% if @task_ownership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_ownership.errors.count, "error") %> prohibited this task_ownership from being saved:</h2>

      <ul>
      <% @task_ownership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.number_field :task_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
